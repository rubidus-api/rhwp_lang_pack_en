feat(i18n): 대화상자에 남은 표시 문자열 영어 표시 (#5852 5단계)

#7186 뒤에도 영어 화면에 한국어로 남던 자리를 옮겼다. 영어로 열어 대화상자를 하나씩
확인해 찾은 것만 담았다(화면에서 보이는 것 기준). src/ui 21개 파일 160곳, 카탈로그
키 1,549 → 1,693개, 영어 100%.

- 선 종류·적용 범위·번호 모양처럼 [값, 표시글] 짝 배열을 풀어 목록을 만드는 자리.
  풀린 이름이 표시 자리로만 가고 값이 기계 자리로만 갈 때에만 옮긴다 — 값을 비교에도
  쓰는 배열(문서에 저장되는 값)은 건드리지 않았다
- 체크상자·라디오 라벨의 createTextNode(' 이름(A)') 자리
- 표시 전용으로 판정된 도우미가 늘었다(label·labeledRowSimple·radioGroup 등): 매개변수
  이름이 문자열 안에 우연히 들어 있어도 '표시 밖 사용' 으로 세지 않고, 제네릭이 붙은
  정의·호출도 읽도록 판정을 고쳤다
- 한글 번호 견본('가,나,다'·'일,이,삼')은 문서에 실제로 찍히는 번호 체계라 그대로 둔다

내부 ID·명령 동작·문서에 저장되는 값은 바꾸지 않았다. 새 의존성 0.
