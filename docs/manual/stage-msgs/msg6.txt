feat(i18n): 남은 대화상자·도구 상자 표시 문자열 영어 표시 (#5852 6단계)

영어로 열어 화면에서 확인한 것만 담았다. 5단계 뒤 영어 화면에 남아 있던 한글이
55종류에서 사라졌고, 남는 것은 옮기면 안 되는 것(글꼴 이름·문서 스타일 이름·
미리보기 견본 글자)뿐이다. src/ui·src/view 283곳과 손 조정, 카탈로그 키
1,693 → 1,947개, 영어 100%.

- [값, 표시글] 배열을 도우미에 넘기는 자리(그림·표 셀 속성의 선 종류·위치 목록 등):
  값은 그대로 두고 표시글만 옮긴다
- 표시 전용으로 확인한 도우미로만 흘러가는 이름 붙은 글자 배열과 Record<string, string> 표
- PR #7278 검토에서 받은 두 가지:
  · 수식 속성 Position/Skew 의 '가로/세로' 는 크기가 아니라 방향이므로 Horizontal/Vertical
  · 구역 설정의 'Continuous' 가 80px 선택 상자에서 잘려 같은 뜻의 'Continue' 로
- 문자열이 곧 타입이던 두 곳은 #7167 처럼 ID 를 먼저 분리했다:
  · TOOLBOX_TARGETS 의 name → nameKey(+ '접기/펴기' 키)
  · zoomPercentShortcutTitle(action: '확대' | '축소') → action: 'zoomIn' | 'zoomOut'
- 무늬 모양 목록 번역 검토 반영: 가로줄/세로줄 Horizontal/Vertical line, X자 Diagonal cross
- 캡션 위치 미리보기 그림(┌가1 등)과 한글 번호 견본은 그대로 둔다

내부 ID·명령 동작·문서에 저장되는 값(스타일 이름·글꼴 이름)은 바꾸지 않았다. 새 의존성 0.
