feat(i18n): 대화상자 영어 표시 (3/4)

src/ui 54개 파일에서 화면에 나가는 문자열 1,021곳을 언어팩으로 옮기고
모듈 최상위 표의 표시글 18개를 이관했다. 영어 100%.

바꿔도 안전하다고 확인한 자리만 건드렸다. 비교식·case·선택자·타입 리터럴은
그대로 뒀다 — 거기서 문자열은 화면이 아니라 값이다.

- 대입의 우변이 식(삼항·??·||·+)이면 그 가지의 문자열도 모두 옮긴다. 대입 바로 뒤
  하나만 옮기면 같은 요소를 다시 쓰는 한국어가 영어 화면에서 번역을 덮는다
- 입력칸 기본값 중 나중에 읽혀 문서로 들어가는 것(새 스타일 이름·누름틀 안내문·
  책갈피 이름)은 옮기지 않는다 — 영어 화면에서 문서 내용이 달라지면 안 된다.
  같은 이유로 클래스 안 name: 모델 기본값도 제외, 모듈 최상위 표의 name: 만 옮긴다
- 앞뒤 줄바꿈을 보존한다 — white-space: pre-line 에서 배치의 일부다
- 키에는 대입 대상이나 감싸는 메서드 이름을 힌트로 넣어 해시만 남지 않게 한다
- 소스의 한글을 정규식으로 단정하던 테스트는 카탈로그-인지 도우미로 옮겼다

새 의존성 0.
