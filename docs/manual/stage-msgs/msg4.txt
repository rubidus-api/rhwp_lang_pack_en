feat(i18n): 명령 레지스트리 영어 표시 (#5852 4단계 2/2)

src/command/commands 9개 파일에서 label: 과 토스트·경고 메시지, 명령 정의 공장
(stub·blockCalcCommand·themeModeCommand·themeSkinCommand)의 이름 인자까지 202곳을 옮겼다.
카탈로그 키 1,549개, 영어 100%.

- 메뉴 막대(2단계)와 레지스트리가 같은 명령을 다른 출처로 들고 있어, 영어 화면에서
  메뉴는 'Open' 인데 Ctrl+Shift+P 팔레트·컨텍스트 메뉴·자동화 API 는 '열기' 였다.
  레지스트리 라벨 중 92개는 마크업이 만든 키(command.file.open.label)를 그대로 쓴다 —
  한 키를 두 곳이 읽으니 어긋날 수 없다. 마크업에 없거나 글이 다른 명령만 registryLabel 키
  (예: 메뉴 '위' / 팔레트 '캡션 - 위')
- 공장 함수는 label 인자가 돌려주는 명령의 label 속성으로만 쓰일 때만 옮긴다. 같은 문장의
  되돌리기 기록 이름('줄/칸 추가' 등)은 건드리지 않았다
- 팔레트 검색(def.label.includes)은 번역된 라벨로 검색되는 것이 맞다
- console.* 안의 로그 문자열은 옮기지 않는다
- command 소스를 정규식으로 단정하던 테스트 3개는 카탈로그-인지 도우미로 옮겼다

새 의존성 0.
